<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.art1001.supply.mapper.role.ProRoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.art1001.supply.entity.role.ProRole">
        <id column="role_id" property="roleId" />
        <result column="role_name" property="roleName" />
        <result column="role_key" property="roleKey" />
        <result column="role_status" property="roleStatus" />
        <result column="role_des" property="roleDes" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="organization_id" property="organizationId" />
        <result column="is_default" property="isDefault" />
        <result column="is_system_init" property="isSystemInit" />
    </resultMap>

    <!-- 获取项目下的角色列表 -->
    <select id="selectProRoles" resultType="com.art1001.supply.entity.role.ProRole">
        SELECT
            role_id,
            role_name,
            role_key,
            role_des,
            create_time,
            is_default,
            is_system_init
        FROM
            sys_pro_role
        WHERE
            project_id = #{projectId}
            AND role_status = 0
        ORDER BY
            create_time
    </select>

</mapper>
