<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.art1001.supply.mapper.content.ReplyMapper">

    <select id="queryByExample" resultType="com.art1001.supply.entity.content.Reply">
        select pr.*,
        tu.image as replyMemberName,
        tu.user_name as replyMemberImage
        from prm_reply pr
        left join tb_user tu
        on pr.reply_member_id = tu.user_id
        ${ew.customSqlSegment}
    </select>
</mapper>