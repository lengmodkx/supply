<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.art1001.supply.mapper.tag.TagMapper">
	
	<resultMap id="BaseResultMap" type="com.art1001.supply.entity.tag.Tag">
		<result column="tag_id" property="tagId" jdbcType="VARCHAR" />
		<result column="tag_name" property="tagName" jdbcType="VARCHAR" />
	</resultMap>
    
	<sql id="Base_Column_List">
						tag_id,
				tag_name
	</sql>

	<sql id="whereStatement">
		<where>
			1 = 1
			<if test="condition !=null">
			</if>
		</where>
	</sql>
	
	<select id="findTagPagerList" resultMap="BaseResultMap"
		parameterType="com.art1001.supply.entity.base.Pager">
		
		select * from prm_tag
		<include refid="whereStatement" />
		
		<if test="orderBy == 0">
			order by tag_id desc
		</if>
		<if test="orderBy == 1">
			order by tag_id asc
		</if>
		
	</select>
	
	<select id="findTagAllList" resultMap="BaseResultMap"
		parameterType="com.art1001.supply.entity.tag.Tag">
		SELECT
		<include refid="Base_Column_List" />
		from prm_tag
		<if test="orderBy == 0">
			order by tag_id desc
		</if>
		<if test="orderBy == 1">
			order by tag_id asc
		</if>
	</select>
	
	<select id="findTagByTagId" resultMap="BaseResultMap" parameterType="java.lang.String">

		select
		<include refid="Base_Column_List" />
		from prm_tag c where tag_id = #{tagId}

	</select>
	
	<delete id="deleteTagByTagId" parameterType="java.lang.String">
		delete from
		prm_tag where tag_id = #{tagId}
	</delete>
	
	<insert id="saveTag" parameterType="com.art1001.supply.entity.tag.Tag">

		insert into prm_tag
		<trim prefix="(" suffix=")" suffixOverrides=",">

			<if test="tagId != null and tagId != ''">
			tag_id,
			</if>
			<if test="tagName != null and tagName != ''">
			tag_name,
			</if>
		</trim>

		<trim prefix="values (" suffix=")" suffixOverrides=",">

			<if test="tagId != null and tagId != ''">
				#{tagId},
			</if>
			<if test="tagName != null and tagName != ''">
				#{tagName},
			</if>
		</trim>

	</insert>
	
	<update id="updateTag" parameterType="com.art1001.supply.entity.tag.Tag">

		update prm_tag
		<set>
			<if test="tagId != null and tagId != ''">
				tag_id = #{tagId},
			</if>
			<if test="tagName != null and tagName != ''">
				tag_name = #{tagName},
			</if>
		</set>

		WHERE tag_id = #{tagId}

	</update>
	
</mapper>